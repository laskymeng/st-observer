# 酒馆观测台 · 使用说明书（v2.0）

> 给非程序员看的操作指南。照着一步步做就行，卡住了随时找 pi 帮忙。

---

## 这套东西是干嘛的

在酒馆里放一个**只看不动**的"监控摄像头"，把测试时发生的事（谁发了消息、AI 回了什么、
世界书亮没亮、有没有报错、变量数值）记下来，让 pi 随时翻看、帮你分析。

**v2 核心升级**：
- **按角色卡分文件**：每张卡独立 `observe_卡名.log`，复盘直达，不用全量扫描
- **提示词分目录**：`prompts/卡名/时间戳_序号.txt`，按卡、按时间直达
- **变量快照变化驱动**：不再定时心跳，仅 character/global 层变化时记录，合法 JSON、无空转
- **世界书完整 entry**：点亮记录含 keys/order/priority/id，不再只有名字
- **Schema v2**：所有事件统一 `_schema: st-observer/v2`，字段全量、可机读

**它不碰任何东西**：不发消息、不改数据、不调用 AI、不连外网。纯旁观。

---

## 项目里有什么（共 5 个文件）

放这些文件的那个文件夹（下面统称 **`st-observer` 文件夹**）

| 文件 | 是什么 | 你要操作吗 |
|---|---|---|
| `启动电话亭.bat` | 一键打开“电话亭”（接收端） | ✅ 每次测试前双击 |
| `酒馆观测台_脚本包.json` | 摄像头的“安装包”，直接导入酒馆助手 | ✅ 第一次导入，之后不用管 |
| `observer.js` | 摄像头源代码（脚本包里已装好它） | 一般不用动，导入失败时备用来手动粘贴 |
| `phonebooth.py` | 电话亭的核心程序 | 不用动，启动器会调它 |
| `使用说明书.md` | 本文档 | —— |

---

## 第一次使用（只需做一遍）

### 第 1 步：把摄像头装进酒馆（约 1 分钟）

1. 打开酒馆（SillyTavern）。
2. 在酒馆页面右侧找 **扩展** 图标（通常是个拼图/插件图标）。
3. 找到 **酒馆助手**，点开它的设置。
4. 找到 **脚本库**，确认顶部选的是 **全局** 那一栏。
5. 点 **导入** 按钮（📄 图标，写着“导入”），选择本文件夹里的 **`酒馆观测台_脚本包.json`**。
6. 导入成功会弹提示“成功导入脚本 ‘酒馆观测台’”。
7. **重要：导入后开关默认是关的**——在脚本列表里找到“酒馆观测台”，把它的**开关打开**。

> 备用方案（如果导入失败）：点 **+ 脚本** 新建一个，用记事本打开 `observer.js` 复制全文粘进去保存，然后同样打开开关。
> 小提示：粘贴的代码不要自己改，除非你知道在改什么。

### 第 2 步：把电话亭准备好（以后每次测试前做）

双击运行 **`启动电话亭.bat`**。会弹出一个黑窗口，显示"电话亭已开启"。**这个窗口要保持开着**——关掉它，摄像头记的东西就送不出去了。

> 平时不用观测时，可以直接关掉电话亭窗口，酒馆完全不受影响。

---

## 平时怎么用

**你想让 pi 帮忙看的时候：**

1. 先双击打开 **电话亭**（黑窗口保持开着）。
2. 正常在酒馆里测试、玩耍。
3. 测完，回来跟 pi 说："**看看刚才观测记录了什么**"。
4. pi 会去读电话亭记下的录像带，给你讲人话。

**用完想停：** 关掉电话亭那个黑窗口即可。观测脚本在酒馆里会一直待命，但你关掉电话亭后它发不出去，也不影响酒馆。

---

## 观测记录存在哪（v2 新结构）

```
st-observer 文件夹/logs/
├── observe_鬼作物语V13.1.log          # 该卡所有事件（JSONL，一行一条）
├── observe_郝叔和他的女人V1.2.log
└── prompts/
    ├── 鬼作物语V13.1/
    │   ├── 2026-09-27_14-30-12_001.txt  # 可读时间戳_序号
    │   └── ...
    └── 郝叔和他的女人V1.2/
        └── ...
```

- **主录像带**：每张卡独立 `observe_卡名.log`（JSONL，一行一条完整事件）
- **提示词全文**：`prompts/卡名/YYYY-MM-DD_HH-mm-ss_序号.txt`，按卡分目录、按时间命名
- 旧版单文件 `observe.log`、`prompts/*.txt` 不再产生（历史文件保留不动）

这些**只存在你电脑本地**，不会上传到任何地方，pi 只是在你要它看时去读一下。

> 注意：`logs/` 目录默认被 `.gitignore` 排除，即使把项目同步到 GitHub 也不会包含你的聊天记录和提示词。

---

## 常见问题

**Q: 双击启动器没反应 / 提示找不到 Python？**
→ 告诉我，我来处理（可能换启动方式）。

**Q: 电话亭窗口关掉后，酒馆会出问题吗？**
→ 完全不会。观测脚本只是"想发但发不出去"，它静默忽略，不影响酒馆任何功能。

**Q: 摄像头会让我花钱吗（AI 算力）？**
→ 不会。它从不发起生成，只在 AI 正常生成时"顺手抄一份"，不多消耗一丝一毫。

**Q: 会偷看我隐私吗？**
→ 只记和测试相关的（事件、提示词、变量、报错），存在本地，不连外网。

**Q: 换电脑 / 重装酒馆怎么办？**
→ 酒馆助手重新装上后，第 1 步再粘一次脚本即可。`st-observer` 这个文件夹可以整体带走。

**Q: v2 怎么看指定卡的记录？**
→ 直接打开 `logs/observe_卡名.log`，或让 pi 说 "看看鬼作物语V13.1 的记录"，pi 会自动读对应文件复盘。

---

## 版本记录

- v2.0：按卡分日志、变量快照变化驱动、字段全量、世界书完整 entry、Schema v2、提示词分目录。
- v1.1：新增 `酒馆观测台_脚本包.json` 导入包（酒馆助手脚本库标准 JSON 格式，导入即装，不再需要手动粘贴）。
- v1.0：首个可用版本。支持观测：消息收发 / 生成过程 / 提示词全文 / 世界书点亮 / 变量快照 / 报错捕获 / 定时快照。